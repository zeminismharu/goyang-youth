# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (인증키가 들어가므로 절대 커밋 금지)
.env
.env.local
.env.*.local
!.env.example

# vercel
.vercel

# typescript (이 프로젝트는 JS만 쓰지만 혹시 생성될 경우 대비)
*.tsbuildinfo
next-env.d.ts

# cloudflare / opennext
.open-next/
.wrangler/
cloudflare-env.d.ts

# wrangler 로컬 시크릿 (절대 커밋 금지)
.dev.vars
.dev.vars.*
